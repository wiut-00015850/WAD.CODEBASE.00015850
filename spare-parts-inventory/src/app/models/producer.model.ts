export interface Producer {
  id: number;            // Unique identifier for the producer
  name: string;         // Name of the producer
  country: string;      // Country of the producer
  foundedDate: Date;    // Date when the producer was founded
}
